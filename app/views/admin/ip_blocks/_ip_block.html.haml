.batch-table__row
  %label.batch-table__row__select.batch-table__row__select--aligned
    = f.check_box :ip_block_ids, { multiple: true, include_hidden: false, data: { checkbox_select_all_target: :checkbox } }, ip_block.id
  .batch-table__row__content.pending-account
    .pending-account__header
      %samp= link_to "#{ip_block.ip}/#{ip_block.ip.prefix}", admin_accounts_path(ip: "#{ip_block.ip}/#{ip_block.ip.prefix}")
      - if ip_block.comment.present?
        ·
        = ip_block.comment
      %br/
      = t("simple_form.labels.ip_block.severities.#{ip_block.severity}")
