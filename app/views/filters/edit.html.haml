- content_for :page_title do
  = t('filters.edit.title')

= simple_form_for @filter, url: filter_path(@filter) do |form|
  = render form, as: :f

  .actions
    = form.button :button, t('generic.save_changes'), type: :submit
