%tr.nested-fields
  %td= form.input :keyword, as: :string
  %td
    .label_input__wrapper= form.input_field :whole_word
  %td
    = form.hidden_field :id if form.object&.persisted? # Required so Rails doesn't put the field outside of the <tr/>
    = link_to_remove_association(f, class: 'table-action-link') do
      = safe_join([material_symbol('close'), t('filters.index.delete')])
