<%= raw t('admin_mailer.new_trends.body') %>

<%= render partial: 'trends/links', object: @links unless @links.empty? %>
<%= render partial: 'trends/tags', object: @tags unless @tags.empty? %>
<%= render partial: 'trends/statuses', object: @statuses unless @statuses.empty? %>
